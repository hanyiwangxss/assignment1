# GitHub Pages deployment

Generated by Prompt 2 on 2026-04-23.

## Where the page lives

- Repository: https://github.com/hanyiwangxss/assignment1
- Live webpage: https://hanyiwangxss.github.io/assignment1/
- Pages source: `main` branch, `/docs` folder.
- Build status at end of Prompt 2: built

## What Prompt 2 changed in this workspace

- Created `docs/` and copied all root `.html` files into it.
- Copied `asia_conflict_analysis_2025_master.html` to `docs/index.html` so Asia conflict analysis is the Pages entry point.
- Created `.gitignore` and added default rules for macOS, editor files, Python, Node, secrets, and large-data patterns.
- Initialized Git repository on `main`, created initial commit, and configured `origin` to GitHub.
- Enabled GitHub Pages from `main` branch and `/docs` path.

## What Prompt 2 did NOT change

- Your `README.md` and every existing file in the project were not modified.
- Your `.py`, `.ipynb`, `.txt`, `.csv`, and `data/` files are untouched.
- Files were copied into `docs/`, not moved. Your root webpage still exists if it was there before.

## How to update the page later

Edit any file, then ask Copilot: *"Commit and push."* The live page rebuilds on its own.

## How to undo

- Disable Pages in the repo settings, or delete the `/docs/` folder and push.
- Delete the repository: `gh repo delete hanyiwangxss/assignment1`.

## Questions

Ask Copilot (me) about any step above. I can explain why each command was run, or help adjust the setup for your specific project.
